# セキュリティポリシー

OpenGreenTrack のセキュリティを重視しています。脆弱性を発見された場合は、以下の手順でご報告ください。

## サポート対象バージョン

本プロジェクトは活発に開発中のため、原則として **`main` ブランチの最新状態**のみをサポート対象とします。

| バージョン | サポート |
|---|---|
| `main`（最新） | ✅ |
| それ以前 | ❌ |

## 脆弱性の報告方法

> ⚠️ **脆弱性は公開の GitHub Issue に書かないでください。** 修正前に情報が公開されると悪用される恐れがあります。

**GitHub Security Advisories** を通じて非公開で報告してください。

本リポジトリの **Security** タブ →  **Report a vulnerability** ボタンから、メンテナだけが閲覧できる非公開の形で報告できます。

報告の際は、可能な範囲で以下を含めていただけると調査が早まります。

- 脆弱性の概要と影響範囲
- 再現手順（PoC があれば）
- 該当するファイル / エンドポイント
- 想定される攻撃シナリオ

## 対応の流れ

1. ご報告を受領しだい、できるだけ早く受付の返信をします。
2. 影響を調査し、修正方針と公開時期をご連絡します。
3. 修正をリリースした後、報告者の同意のもとで謝辞を掲載します。

## 秘密情報の取り扱いについて（コントリビューター向け）

本プロジェクトは Supabase を利用しており、キーの取り扱いを誤ると重大なインシデントにつながります。

- `SUPABASE_SERVICE_ROLE_KEY`（管理者権限キー）を、**クライアントコンポーネント・`NEXT_PUBLIC_` 変数・コミットするファイルに書かない**でください。
- `.env.local` はコミットしないでください（コミット可能なのは `.env.example` のみ）。
- 万一シークレットをコミット・push してしまった場合は、**直ちに該当キーをローテーション（無効化・再発行）**し、上記の経路で連絡してください。履歴から消すだけでは漏洩は解消しません。

詳細は [`AGENTS.md`](AGENTS.md) の R7 を参照してください。
